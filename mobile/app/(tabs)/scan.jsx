// Placeholder for the center "Scan" tab — never actually renders; the tabBarButton
// in (tabs)/_layout.jsx intercepts the press and routes to /scanner.
export default function Scan() {
  return null;
}
